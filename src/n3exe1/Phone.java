package n3exe1;

public interface Phone {
	
	public String call (int number);
	
	/*
	 * Para realizar este ejercicio necesitas una interfaz llamada Telefono (OK)
	 * y tres clases: Smartphone, Generica y la Principal. (OK)
	 * La interfaz de teléfono debe tener un método llamado llamar(). (OK)
	 * La clase Smartphone debe implementar Telefono, y además del método llamar(), (OK)
	 * también debe tener el método hacerFotos(). (OK)
	 * La clase Generica debe tener dos métodos genéricos, (OK) 
	 * el primero debe recibir un tipo de argumento limitado 
	 * por la interfaz Telefón y el segundo debe recibir un argumento 
	 * limitado por la clase Smartphone. (OK) Desde estos dos métodos llama a 
	 * los métodos pertinentes de Telefono y Smartphone. (OK)
	 * En el main() de la clase Principal, pasa un objeto de la clase Smartphone 
	 * a los dos métodos genéricos de la clase Generica.
	 * ¿El método limitado por la interfaz Telefon, 
	 * de la clase Generica, puede llamar al método hacerFotos()?
	 */

}
